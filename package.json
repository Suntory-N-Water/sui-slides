{
  "name": "sui-slides-template",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "marp -s slides --html --allow-local-files",
    "build": "marp slides --html --allow-local-files",
    "pdf": "marp --pdf --allow-local-files"
  },
  "dependencies": {
    "@marp-team/marp-cli": "^4.2.3",
    "@marp-team/marp-core": "^4.2.0"
  }
}
